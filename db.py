# This file has been superseded by the db/ package (db/models.py + db/session.py).
# Python 3 prefers the db/ package over this module, so this file is never imported.
# It is kept only to avoid confusing git diffs — safe to delete.
raise ImportError("db.py is obsolete. Use `import db` (the db/ package).")
